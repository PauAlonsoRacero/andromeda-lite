[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = backend
python_files = test_*.py
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    -p no:warnings
    -m "not slow"
markers =
    asyncio: async tests
    slow: tests lentos (estrés a escala); correr con -m slow
