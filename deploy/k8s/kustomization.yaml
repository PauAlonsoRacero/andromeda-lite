apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: andromeda
resources:
  - 00-namespace.yaml
  - 10-configmap.yaml
  - 20-ollama.yaml
  - 30-backend.yaml
  - 40-frontend.yaml
  - 50-ingress.yaml
  - 60-hpa.yaml
  - 70-pdb.yaml
  - 90-networkpolicy.yaml
  # 80-servicemonitor.yaml requiere el CRD del Prometheus Operator; se aplica
  # aparte si kube-prometheus-stack está instalado:
  #   kubectl apply -f 80-servicemonitor.yaml
