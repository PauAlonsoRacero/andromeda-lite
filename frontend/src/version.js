// Versión central de Andromeda (frontend).
// Debe coincidir con app_version del backend (backend/app/config.py).
export const APP_VERSION = '1.0.0'
export const APP_CHANNEL = 'beta'
export const APP_BUILD = '20260623'   // fecha de este build — sirve para verificar que el binario es reciente
export const APP_VERSION_LABEL = `v${APP_VERSION}-${APP_CHANNEL}`
